applications:
- opencontrail
parameters:
  _param:
  opencontrail:
    common:
      version: 3.0
      identity:
        engine: k8s
        host:
        port: 35357
        token:
        password:
      network:
        engine: k8s
        host:
        port: 9696
    compute:
      version: 3.0
      enabled: True
      discovery:
        host: ${_param:kubernetes_network_node01_single_address}
      interface:
        address: ${_param:single_address}
        dev: eth0
        gateway: 10.0.174.1
        mask: 23
        dns: 8.8.8.8
        mtu: 9000